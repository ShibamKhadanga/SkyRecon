# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
aiofiles>=24.1.0
httpx>=0.27.0                   # Async HTTP client for SSE stream proxy

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.35
psycopg2-binary>=2.9.10         # PostgreSQL adapter (binary, no build tools needed)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── AI / Computer Vision ───────────────────────────────────────────────────────
ultralytics>=8.3.0              # YOLOv8 + ByteTrack (built-in tracker)
opencv-python-headless>=4.10.0  # Headless: no GUI deps needed on server
numpy>=1.26.0
torch>=2.3.0
torchvision>=0.18.0
transformers>=4.40.0            # SegFormer-B2 (aerial segmentation) + CLIP (Object Finder)
accelerate>=0.30.0              # Required for HuggingFace GPU inference (SegFormer)
Pillow>=10.0.0                  # Image processing (SegFormer pipeline + report screenshots)

# ── Report Generation ──────────────────────────────────────────────────────────
reportlab>=4.2.0                # PDF generation
python-docx>=1.1.0              # DOCX generation
jinja2>=3.1.0

# ── Deployment Utilities ───────────────────────────────────────────────────────
python-dotenv>=1.0.0            # Load .env on Render / Railway / local

# ── ONNX (installed automatically by ultralytics export, listed for reference) ─
# onnx>=1.12.0
# onnxruntime>=1.16.0
